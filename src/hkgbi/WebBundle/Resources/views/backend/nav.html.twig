{% for module in menu %}
        <li style="padding-right: 10px">
            <a href="{{ path('article_list',{'identifier':module.identifier}) }}" title="{{ module.name }}">{{ module.name }}</a>
        </li>
{% endfor %}