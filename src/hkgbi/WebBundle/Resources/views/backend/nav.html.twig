{% for module in menu %}
        <li style="padding-right: 10px">
            <a href="{{ path('redirecttomodule',{'module_identifier':module.identifier}) }}" title="{{ module.name }}">{{ module.name }}</a>
        </li>
{% endfor %}